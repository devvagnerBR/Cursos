let sun;
sun.name = 'Sol';
sun.mass = 1.1989 * (Math.pow(10, 30));
sun.age = 4.603 * (Math.pow(10, 9));
sun.planets = [];
let asteroid;
asteroid.name = 'XXXXXX';
class MILKYWAYPLANET {
    constructor(name, mass, population) {
        this.createSatellite = (name) => {
            //.
        };
        this.name = name,
            this.mass = mass,
            this.population = population;
    }
}
